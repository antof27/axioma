services:
  music-hub:
    build: .
    container_name: music-hub
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - DATABASE_DIR=/app/data
